name: sustainity_api
description: API definitions for sustainity.dev API.
version: 1.0.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  collection: ^1.17.1
  equatable: ^2.0.0
  http: 0.13.5
  json_annotation: ^4.8.0
  json_serializable: ^6.5.4

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  build_runner: ^2.1.0
